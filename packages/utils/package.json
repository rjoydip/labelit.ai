{
  "name": "@labelit/utils",
  "version": "0.0.0",
  "main": "./src/index.ts",
  "exports": {
    ".": {
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "lint": "oxlint . --fix",
    "format": "oxfmt . --write",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "typecheck": "tsc --noEmit",
    "update": "bun update"
  },
  "devDependencies": {
    "oxfmt": "^0.46.0",
    "oxlint": "^1.61.0",
    "typescript": "^5.8.2",
    "vitest": "^4.1.5"
  }
}
